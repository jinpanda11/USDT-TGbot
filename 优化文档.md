# USDT 收入分析项目优化文档

> 审阅范围：Telegram Bot、TronGrid 查询、JSON 存储、旧版网页、Docker/Watchtower、GitHub Actions。
>
> 审阅日期：2026-08-10
>
> 优先级说明：P0 = 可能造成统计错误、数据丢失或密钥泄漏；P1 = 明显影响生产可用性或运维安全；P2 = 质量、性能和可维护性改进；P3 = 长期架构演进。

## 一、当前项目概况

项目已经具备可用的 MVP 能力：

- Telegram Bot 和旧版网页均支持 TRON USDT(TRC20) 月度收入查询。
- 支持多地址、地址标签、排除自有地址互转、CSV 导出和 USDT/CNY 汇率。
- TronGrid 查询包含分页、超时、重试和地址级并发控制。
- Bot 使用北京时间计算月份，金额使用 `BigInt` 微单位累计，避免了大部分浮点累加误差。
- 用户配置按 Telegram 用户 ID 隔离，并使用临时文件替换写入 `users.json`。
- Docker 镜像、GHCR 发布和 Watchtower 自动更新链路已经建立。

当前主要短板不在功能数量，而在数据正确性、安全边界、可观测性和生产级测试不足。

## 二、优先处理清单

### P0：先修统计正确性和敏感数据安全

| 问题 | 位置 | 影响 | 建议 |
|---|---|---|---|
| TRON 地址只做长度/字符集校验，没有 Base58Check 校验 | `src/trongrid.js:20-22`、`src/bot.js:477` | 被篡改的无效地址仍可能添加成功，查询结果可能为空或误导用户 | 使用成熟的 TRON 地址库，或实现 Base58Check + 公钥版本校验；网页端复用同一规则 |
| 用户 TronGrid API Key 明文保存 | `src/storage.js:77-82`、`data/users.json` | 服务器文件、备份、日志或误操作泄露后可直接滥用 API 配额 | 优先改为服务器端统一 Key + Telegram 用户白名单；若必须支持个人 Key，使用 Secret Manager 或加密存储，并限制备份访问权限 |
| 未限制 Bot 使用者和聊天类型 | `src/bot.js:451-842` | 任何人都可能消耗默认 Key 的配额；群聊中发送 Key、地址和收入信息存在泄漏风险 | 增加 `ALLOWED_TELEGRAM_USER_IDS`；敏感命令仅允许私聊；拒绝群组中的 `/setkey`、`/query`、`/export` 等操作 |
| 存储文件损坏时会以空数据继续运行 | `src/storage.js:25-38` | 下一次写入可能覆盖原始数据，造成用户地址、Key 和配置丢失 | 解析失败时停止写入并告警；保留 `.bak`；启动时做 JSON schema 校验和恢复流程 |
| 交易结果没有按交易 ID 去重 | `src/trongrid.js:99-117、157-202` | 分页接口重复、接口异常或重试边界变化时，可能重复计入收入 | 以 `transaction_id + to + value` 建立去重键；记录去重数量并加入测试 |

### P1：提升生产可用性和运维安全

| 问题 | 位置 | 影响 | 建议 |
|---|---|---|---|
| 配置数值缺少范围校验 | `src/config.js:18-21` | 负并发、负超时、异常重试次数可能导致查询不执行、立即超时或异常行为 | 启动时统一校验：并发 1-20、超时 1-120000ms、重试 0-5、汇率大于 0；错误信息要指出变量名和值 |
| 整月所有交易先放入内存 | `src/trongrid.js:99-117、153-202` | 高频地址或大月份可能造成内存增长、CSV 过大、Telegram 发送失败 | 查询过程中聚合；导出使用临时文件或流式写入；设置最大页数/最大记录数并给出可读告警 |
| 所有用户共享外部 API 请求资源 | `src/bot.js:334-447`、`src/trongrid.js:120-129` | 多用户同时查询时容易触发 TronGrid/CoinGecko 限流，Bot 响应变慢 | 增加全局并发上限、按用户限流和相同查询缓存；处理 HTTP 429 的 `Retry-After` |
| Docker 容器以 root 运行 | `Dockerfile:12-18` | Bot 或依赖被利用后，容器内权限过高；bind mount 数据文件也可能被 root 所有 | 创建固定 UID/GID 的非 root 用户；部署前用 `chown` 解决 `/app/data` 权限，而不是长期以 root 运行 |
| Watchtower 挂载 Docker Socket | `docker-compose.yml:14-28` | Docker Socket 基本等价于宿主机管理权限，属于高风险运维边界 | 优先改为 CI 发布后由受控部署流程更新；必须使用 Watchtower 时，使用 socket-proxy、固定镜像摘要和最小权限主机 |
| 默认使用可变 `latest` 镜像 | `docker-compose.yml:2`、`.github/workflows/docker-publish.yml:39` | 无法保证每次部署的确切版本，回滚和审计困难 | 生产环境使用 `sha-...` 或镜像 digest；保留 `latest` 仅作开发/演示标签 |
| 没有健康检查和启动存活信号 | `Dockerfile`、`docker-compose.yml` | 进程卡死但未退出时，`restart: always` 不会修复；外部无法判断 Bot 是否健康 | 增加 `/health` 或本地健康检查；至少检查进程、Telegram API 连通性和数据目录可写性 |
| CI 只构建镜像，不执行测试和安全扫描 | `.github/workflows/docker-publish.yml:19-52` | 有缺陷的代码可以直接发布到生产，供应链风险不可见 | 发布前执行 `npm ci`、语法检查、单元测试、依赖审计、镜像漏洞扫描；构建后生成 SBOM，必要时签名镜像 |

### P2：改善质量、可维护性和用户体验

| 问题 | 位置 | 影响 | 建议 |
|---|---|---|---|
| 没有自动化测试 | `package.json` 仅有 `start/dev` | 月份边界、分页、去重、排除自转、金额和 CSV 回归容易发生 | 引入 Node 内置 `node:test`，先覆盖 `trongrid.js` 和 `storage.js`，再补 Bot handler 集成测试 |
| Bot、查询和存储职责集中 | `src/bot.js` 约 848 行 | 修改按钮或命令容易影响查询流程，难以独立测试 | 拆成 `handlers/`、`services/income-service.js`、`repositories/storage.js`、`ui/keyboard.js` |
| API Key 探测逻辑硬编码 URL 和地址 | `src/bot.js:313-326` | 自定义 `TRONGRID_API_BASE` 时探测行为不一致；无法统一复用超时/重试配置 | 把 Key 验证封装到 TronGrid 客户端，复用配置、错误格式化和请求封装 |
| 会话状态只在内存中保存且没有过期时间 | `src/bot.js:84-103、624-697` | 重启后交互中断；长期未完成会话会占用内存 | 增加 10-30 分钟 TTL 和清理任务；必要时把短会话持久化或允许用户重新开始 |
| 错误日志可能包含过多上下文，且无结构化日志 | `src/bot.js:440`、`src/trongrid.js:192` | 线上排查困难，也可能把请求细节带入日志 | 使用 JSON 日志，统一 request/user hash、月份、地址脱敏、耗时和错误类型；严禁记录 API Key |
| 汇率换算仍转成 Number | `src/trongrid.js:215-223` | 超大金额时存在精度损失，汇总金额与明细可能出现极小偏差 | 使用十进制定点计算，或用微单位与汇率缩放后再格式化；明确汇率来源和抓取时间 |
| Telegram 消息长度和 CSV 大小没有边界处理 | `src/bot.js:413-437` | 地址多、记录多时可能超过 Telegram 消息/文件限制 | 汇总只展示固定上限；CSV 分片或压缩；对超限给出明确提示和下载方式 |
| 网页版与 Bot 重复实现业务逻辑 | `usdt-收入分析.html`、`src/trongrid.js` | 两套地址校验、分页和错误处理会逐渐产生行为差异 | 保留网页作为静态归档，或将业务逻辑抽成共享包/统一后端 API；至少同步修复两端校验规则 |
| 文档与实际配置存在维护成本 | `README.md`、`.env.example`、Compose | 新部署者容易误用 `latest`、root 或 Watchtower Socket | 增加生产部署清单、备份恢复演练、版本回滚示例和安全边界说明 |

## 三、推荐实施路线

### 阶段 1：正确性与数据安全（优先上线前完成）

1. 引入真正的 TRON 地址校验，并为地址添加测试。
2. 对 `users.json` 做 schema 校验、损坏保护、备份和恢复；写入前使用临时文件并同步文件权限。
3. 增加私聊限制和用户白名单，明确是否允许个人 API Key。
4. 为交易按 `transaction_id` 去重，并校验合约地址、收款地址、时间范围和金额格式。
5. 增加配置启动校验，非法配置直接 fail fast。

**验收标准：** 无效 TRON 地址无法添加；损坏数据文件不会被空数据覆盖；未授权用户无法触发查询；同一交易重复出现在响应中只计一次。

### 阶段 2：测试、限流与可观测性

1. 使用 `node:test` 建立单元测试和模拟 TronGrid HTTP 响应。
2. 测试北京时间月初/月末、分页、空页、重复页、429/5xx、超时、部分地址失败和排除自转。
3. 增加全局并发、每用户限流和查询超时；为长查询提供取消或过期机制。
4. 增加结构化日志、耗时指标、成功/失败地址数、去重数和限流次数。
5. 为 Bot 增加最小健康检查和优雅退出验证。

**验收标准：** CI 在构建镜像前必须通过测试；一次异常不会阻塞其他地址；可以从日志定位用户、月份、失败地址和错误类型，但看不到 API Key。

### 阶段 3：容器和发布链路加固

1. 以非 root 用户运行容器，补充 `HEALTHCHECK`。
2. 生产 Compose 固定镜像 digest 或 SHA 标签，不使用可变 `latest`。
3. CI 增加 `npm audit --omit=dev`、镜像扫描、SBOM 和构建元数据。
4. 评估移除 Watchtower；若保留，使用 socket-proxy，并限制只更新带标签的目标容器。
5. 建立自动备份、恢复演练和最近一次可用镜像的回滚记录。

**验收标准：** 容器内进程不以 root 运行；健康检查失败可被发现；发布记录能还原源码提交、镜像摘要和依赖版本；恢复演练可以找回 `users.json`。

### 阶段 4：数据层和架构升级

当地址数量、用户数量或查询频率继续增长时，再考虑以下升级：

- 用 SQLite 替代 `users.json`，开启 WAL，给用户、地址、查询任务、交易记录建立表和索引。
- 将查询改为后台任务，结果落库，Bot 只负责提交任务和读取结果。
- 增加增量同步：按最后确认的区块/时间游标查询，而不是每次重扫整月。
- 使用 Redis 或数据库实现跨进程锁、限流和任务状态。
- 将 TronGrid 客户端、汇率客户端和 Telegram 展示层解耦，便于替换节点供应商。

## 四、建议的测试矩阵

| 模块 | 必测场景 |
|---|---|
| 地址校验 | 正确地址、校验和错误、长度错误、非法 Base58 字符、前后空格 |
| 时间范围 | 北京时间月初 00:00、月末 23:59:59、跨 UTC 日期、闰年二月 |
| TronGrid 分页 | 多页、空页、重复 `next`、缺失 `next`、异常 JSON、429、500、超时 |
| 交易过滤 | 非 USDT 合约、非目标地址、时间越界、负金额、非法金额、重复交易、自有地址互转 |
| 汇率 | 实时成功、CoinGecko 失败、个人汇率、默认汇率、非法配置、超大金额 |
| 存储 | 首次创建、更新、并发写、损坏 JSON、未知字段、历史数据兼容、备份恢复 |
| Bot | 未授权用户、群聊敏感命令、重复查询、查询中再次查询、会话超时、导出超限 |
| 部署 | 非 root、数据目录不可写、健康检查失败、镜像回滚、Watchtower 权限边界 |

## 五、配置和部署建议

建议在生产环境至少增加以下配置：

```text
ALLOWED_TELEGRAM_USER_IDS=123456789,987654321
REQUIRE_PRIVATE_CHAT=true
GLOBAL_QUERY_CONCURRENCY=2
MAX_PAGES_PER_ADDRESS=100
MAX_RECORDS_PER_QUERY=100000
SESSION_TTL_MS=1800000
LOG_LEVEL=info
```

配置解析要做到：

- 所有数字都有明确上下限，非法值启动即失败。
- Key、Token 只从环境变量或 Secret Manager 读取，不写入普通日志。
- 生产环境不使用 `latest`，并记录当前镜像 digest。
- `data/` 和 `.env` 使用最小文件权限，备份文件加密并定期验证可恢复性。

## 六、已完成的本次检查

- `src/*.js` 已通过 `node --check` 语法检查。
- `npm audit --omit=dev` 当前报告 `0 vulnerabilities`。
- `npm outdated` 显示 `dotenv` 有可升级的大版本（当前 16.x，最新 17.x）；升级前应先跑完整测试。
- 当前环境未安装 Docker，因此未能实际执行 `docker compose config`、镜像构建和容器启动验证。

## 七、结论

这个项目已经可以作为小规模、低频使用的 Bot MVP 运行，但还不适合在没有访问控制、备份恢复和自动化测试的情况下长期承载生产数据。最值得优先投入的不是继续增加命令，而是：

1. 修复 TRON 地址校验和交易去重，保证统计可信。
2. 限制 Bot 使用范围并保护 API Key，降低泄露和配额滥用风险。
3. 防止损坏数据覆盖、补齐备份恢复和启动配置校验。
4. 建立测试、限流、日志和健康检查，再加固容器发布链路。

完成阶段 1 和阶段 2 后，项目的可靠性会有明显提升；只有当用户量和数据量增长到 JSON 存储成为瓶颈时，再迁移 SQLite/任务队列，避免过早引入不必要的基础设施。
