# Telegram USDT 收入分析 Bot 广告功能开发文档

> 文档类型：功能开发设计稿
>
> 适用项目：TRON USDT 收入分析 Telegram Bot
>
> 编写日期：2026-08-10
>
> 目标：在不影响收入统计可信度和用户隐私的前提下，增加可配置、可审计、可退订的广告能力。

## 一、功能目标与边界

### 1.1 目标

广告功能需要满足以下目标：

- 支持在查询结果中展示低干扰的赞助内容。
- 为未来的主动广告推送预留完整的数据和任务模型。
- 广告内容、投放时间、目标链接和发送状态可追踪。
- 商业消息必须明确标记，不能与收入分析结果混淆。
- 用户可以主动关闭商业消息，并且退订立即生效。
- 广告发送失败、限流、用户拉黑 Bot 等情况可重试或自动处理。
- 管理员操作需要权限控制、二次确认和审计记录。

### 1.2 非目标

第一期不实现以下内容：

- 不根据用户收入金额、钱包余额、交易对手或交易明细进行广告定向。
- 不自动读取或分析用户的链上财务画像来决定广告内容。
- 不在群组中主动发送商业广告。
- 不允许广告主直接调用 Bot API 发送内容。
- 不接入未审核的高收益、保本、空投、私钥或助记词相关推广。
- 不把服务故障公告和商业广告混为同一类消息。

## 二、推荐产品路线

### 阶段 A：查询结果赞助位

用户主动执行 `/query` 或 `/export` 后，在结果末尾显示一条广告或赞助内容。

示例：

```text
📊 2026-08 收入汇总（北京时间）
合计：12,345.00 USDT
约合：¥88,884.00
笔数：136

────────────
📢 赞助内容
XXX 钱包：支持 TRON 多地址管理与资产提醒
[了解详情]
```

特点：

- 不需要批量广播，开发和运维成本最低。
- 只有用户主动查询时才曝光，不打扰用户。
- 不容易触发 Telegram 全局发送限流。
- 可按查询次数统计曝光量和点击量。
- 适合作为第一版上线功能。

### 阶段 B：用户自愿订阅商业消息

用户主动开启后，才接收商业广告推送。

建议交互：

```text
⚙️ 设置
商业消息：关闭
[开启商业消息]
```

支持：

- `/marketing on`：开启商业消息。
- `/marketing off`：关闭商业消息。
- 每条商业消息附带“退订广告”按钮。
- 设置发送频率上限，例如每周不超过 2 条。
- 用户屏蔽 Bot 或发送失败后自动标记不可达。

### 阶段 C：管理员广告活动管理

管理员可以创建、预览、排期、暂停和查看广告活动，但任何面向用户的批量推送都必须经过明确确认。

## 三、当前项目需要调整的架构

当前项目以 `src/bot.js` 为主要命令和交互入口，以 `src/storage.js` 将用户数据保存到 `data/users.json`。这个结构可以支持阶段 A，但不适合直接承载可靠的批量推送。

### 3.1 阶段 A 的最小改造

可以暂时继续使用 JSON 存储，增加独立的广告配置模块：

```text
src/
├── ad-service.js       # 广告选择、过滤、曝光记录
├── ad-renderer.js      # Telegram 文本和按钮渲染
├── ad-config.js        # 广告配置读取和校验
├── bot.js
├── storage.js
└── trongrid.js
```

广告服务不应该直接嵌入 `queryMonthIncome`。查询服务只返回统计结果，Bot 展示层在发送结果时追加广告。

### 3.2 阶段 B/C 的推荐改造

在正式支持推送前，建议将 `users.json` 迁移到 SQLite。至少拆分为：

```text
src/
├── bot/
│   ├── commands.js
│   ├── callbacks.js
│   └── keyboards.js
├── services/
│   ├── income-service.js
│   ├── ad-service.js
│   ├── campaign-service.js
│   └── delivery-service.js
├── repositories/
│   ├── user-repository.js
│   ├── campaign-repository.js
│   └── delivery-repository.js
├── jobs/
│   └── campaign-worker.js
└── storage.js
```

广告投放应采用持久化任务队列，不能依赖一次循环直接发送。这样容器重启后可以恢复未完成任务，并且能够记录每个用户的成功、失败和重试状态。

## 四、数据模型

### 4.1 用户字段

在用户记录中增加以下字段：

```text
telegram_user_id       Telegram 用户 ID，主键
chat_id                私聊 Chat ID
marketing_opt_in       是否同意接收商业消息，默认 false
marketing_opt_in_at    同意时间
marketing_opt_out_at   退订时间
last_interaction_at    最近交互时间
bot_blocked_at         用户屏蔽 Bot 的时间
status                 active / blocked / disabled
```

注意：`marketing_opt_in` 必须默认 `false`，不能因为用户使用了查询功能就自动视为同意广告推送。

### 4.2 广告内容

```text
ads
- id
- title
- body
- sponsor_name
- button_text
- target_url
- status                 draft / pending_review / approved / paused / expired
- placement              query_result / broadcast / both
- starts_at
- ends_at
- max_impressions
- created_by
- created_at
- updated_at
```

校验要求：

- `body` 限制最大长度，避免超过 Telegram 消息限制。
- `target_url` 只允许 `https`，并使用域名白名单。
- `button_text` 限制短文本长度。
- 广告必须有 `sponsor_name`，并明确显示“广告”或“赞助内容”。
- 广告下线时间必须晚于上线时间。
- 审核通过后，正文、链接和投放范围不允许无审计修改。

### 4.3 广告活动

```text
campaigns
- id
- ad_id
- type                   result_slot / opt_in_broadcast
- status                 draft / scheduled / running / paused / completed / cancelled
- scheduled_at
- ended_at
- audience_rule          opt_in_only / all_active_for_service_notice
- frequency_limit
- created_by
- approved_by
- created_at
```

商业广告只能使用 `opt_in_only`。`all_active_for_service_notice` 只允许服务公告使用，不得用于商业广告。

### 4.4 发送记录

```text
campaign_deliveries
- id
- campaign_id
- user_id
- status                 pending / sent / failed / skipped / blocked
- attempts
- telegram_message_id
- last_error_code
- last_error_message
- sent_at
- created_at
- updated_at
```

建议为 `(campaign_id, user_id)` 建唯一索引，防止同一活动重复发送给同一用户。

### 4.5 曝光和点击记录

```text
ad_events
- id
- ad_id
- user_id
- event_type             impression / click / opt_in / opt_out
- source                 query_result / broadcast / settings
- created_at
```

只记录统计所需的最小数据。不要把钱包地址、收入金额、付款地址等链上明细写入广告事件。

## 五、阶段 A：查询结果广告位设计

### 5.1 展示规则

- 只在查询成功或部分成功时展示。
- 查询失败时不展示广告，避免用户误认为广告与失败原因有关。
- 查询结果中最多展示一个广告。
- 广告和统计结果之间使用明确分隔线。
- 广告必须带“广告”或“赞助内容”标签。
- 不修改合计、约合金额、笔数和明细内容。
- 没有符合条件的广告时不显示空广告容器。

### 5.2 广告选择规则

```text
1. 过滤 status=approved 的广告。
2. 过滤 placement 包含 query_result 的广告。
3. 过滤当前时间在 starts_at 和 ends_at 之间的广告。
4. 过滤达到 max_impressions 的广告。
5. 按优先级或轮播权重选择一个广告。
6. 记录 impression 事件。
```

第一版可以使用简单轮播，不需要复杂的竞价系统。广告选择失败不能影响收入查询结果。

### 5.3 按钮和链接

- 使用 Telegram Inline Keyboard。
- 只允许 HTTPS 链接。
- 对外链使用白名单域名。
- 点击事件使用 `callback_data` 或短链标识，不把完整敏感参数放进按钮回调。
- 记录点击时只保存广告 ID、用户 ID 和来源，不保存链上查询结果。

### 5.4 统计口径

至少统计：

- 展示次数：广告消息成功发送后记录。
- 点击次数：Telegram 回调或受控跳转页确认后记录。
- 查询用户数：按去重用户 ID 统计。
- CTR：点击次数 / 展示次数。

展示记录必须在 Telegram 发送成功后写入，不能在发送前记录，否则发送失败会虚增曝光量。

## 六、阶段 B：商业消息订阅设计

### 6.1 订阅流程

1. 用户进入设置页。
2. 点击“开启商业消息”。
3. 展示说明：消息类型、预计频率、退订方式和隐私说明。
4. 用户再次点击确认。
5. 写入 `marketing_opt_in=true` 和同意时间。
6. 回复订阅成功，并提供“关闭商业消息”按钮。

不能通过默认勾选、模糊文案或使用 `/start` 自动完成同意。

### 6.2 退订流程

退订入口必须至少有三处：

- 设置页开关。
- 每条商业广告底部的“退订广告”按钮。
- `/marketing off` 命令。

退订后：

- 立即停止该用户所有未发送的商业广告任务。
- 不再进入新的商业活动受众列表。
- 保留退订时间和审计记录。
- 不影响 `/query`、`/export` 等核心服务消息。

### 6.3 频率限制

默认建议：

- 每个用户每周最多 2 条商业广告。
- 同一广告活动每个用户最多 1 条。
- 两条商业广告之间至少间隔 48 小时。
- 服务公告不计入商业广告频率，但仍应低频发送。

频率限制需要在数据库查询层实现，不能只在内存 Set 中实现，否则容器重启后会失效。

## 七、阶段 C：管理员操作

### 7.1 管理员身份

增加环境变量：

```text
ADMIN_TELEGRAM_USER_IDS=123456789,987654321
```

所有广告管理命令和回调都必须检查管理员 ID。不要仅凭用户名、昵称或群组管理员身份判断权限。

### 7.2 建议命令

```text
/ad_new                 创建草稿
/ad_list                查看广告
/ad_preview <id>        预览广告
/ad_approve <id>        审核通过
/ad_pause <id>          暂停广告
/ad_schedule <id>       设置投放时间
/ad_stats <id>          查看曝光和点击
/campaign_start <id>    启动活动
/campaign_pause <id>    暂停活动
```

正式批量发送前必须：

- 显示广告正文、按钮链接、受众数量、预计发送量和频率限制。
- 要求管理员二次确认。
- 生成操作日志。
- 支持暂停活动。

### 7.3 广告审核清单

审核者需要确认：

- 广告主身份和联系方式。
- 最终落地链接和所有跳转链路。
- 是否存在私钥、助记词、授权签名或异常转账诱导。
- 是否存在保本、高收益、虚假空投等误导内容。
- 是否与项目核心收入分析结果产生混淆。
- 是否符合目标地区的广告、金融和隐私法规。

## 八、发送队列和 Telegram 限流

### 8.1 发送流程

```text
创建活动
  -> 生成 pending 投递记录
  -> Worker 读取一批任务
  -> 检查用户仍然 opt-in
  -> 检查频率限制
  -> 发送 Telegram 消息
  -> 成功则记录 sent 和 message_id
  -> 429 则按照 retry_after 延迟重试
  -> 用户 blocked 则标记 blocked，不再重试
  -> 其他错误按退避策略重试，超过上限标记 failed
```

### 8.2 发送限制

- 设置全局发送速率，避免瞬时批量发送。
- 发送任务分批执行，不要一次性加载全部用户。
- 每次发送前重新检查订阅状态。
- 处理 Telegram `429 Too Many Requests` 的 `retry_after`。
- 对网络错误使用指数退避和最大重试次数。
- Bot 重启后从 `pending` 和可重试的 `failed` 任务继续。

### 8.3 幂等性

发送前先检查 `(campaign_id, user_id)` 是否已经是 `sent`。

如果 Telegram 已发送但本地写入失败，可能出现重复发送。可以通过消息幂等标识、发送窗口状态和人工审计降低概率，但 Telegram Bot API 本身不提供完全可靠的消息幂等键，因此必须接受并监控极小概率的重复发送。

## 九、安全、隐私和合规要求

### 9.1 明确区分消息类型

消息类型至少分为：

- `service`：查询结果、系统故障、版本升级等服务消息。
- `marketing`：商业广告、赞助内容、优惠推广。

服务消息不能被用作绕过商业消息退订的方式。

### 9.2 最小化数据

广告系统不得读取或使用以下字段进行定向：

- 收入合计。
- USDT 余额。
- 付款地址。
- 用户管理的收款地址。
- 交易频率和交易对手。

可以使用的基础规则仅限于：是否主动订阅、语言、时区、国家/地区（在用户明确提供且有合法依据时）。

### 9.3 密钥和日志

- 不记录 Telegram Bot Token、TronGrid API Key 和完整外链签名参数。
- 管理员预览时对 Key、Token 和用户 ID 做脱敏。
- 广告事件和发送日志设置保留期限。
- 备份文件加密，访问权限最小化。
- 如果使用外部广告平台，必须先确认其数据处理和缓存策略。

## 十、测试计划

### 10.1 单元测试

- 广告时间窗口判断。
- 状态过滤和轮播选择。
- 达到展示上限后的停止投放。
- 每周频率限制和 48 小时间隔。
- 订阅、退订状态转换。
- 域名白名单和 HTTPS 校验。
- Telegram 文本长度限制。
- 广告消息渲染和按钮回调数据。

### 10.2 集成测试

- 查询成功后正确追加一个广告。
- 查询失败时不追加广告。
- 广告选择异常时查询结果仍正常返回。
- 用户退订后，未发送队列被跳过。
- Telegram 429 按 `retry_after` 重试。
- 用户屏蔽 Bot 后标记为 `blocked`。
- 容器重启后未完成任务继续执行。
- 同一个活动不会重复发送给同一个用户。

### 10.3 管理员流程测试

- 非管理员执行 `/ad_*` 全部拒绝。
- 草稿不能直接投放。
- 未审核广告不能启动活动。
- 暂停活动后不再产生新的发送请求。
- 修改广告链接后留下审计记录。
- 活动统计与发送记录数量一致。

## 十一、上线和回滚方案

### 11.1 灰度发布

1. 先只上线广告数据模型和关闭状态，不展示广告。
2. 仅对管理员账号展示测试广告。
3. 开启阶段 A，广告展示比例从 10% 开始。
4. 观察查询失败率、响应耗时、用户退订和点击数据。
5. 确认不影响统计后，再逐步扩大展示比例。
6. 阶段 B 必须另行公告，并默认关闭商业消息订阅。

### 11.2 回滚开关

增加全局开关：

```text
ADS_ENABLED=false
MARKETING_BROADCAST_ENABLED=false
```

关闭广告时：

- 查询核心功能继续工作。
- 不删除广告和发送记录。
- 暂停未执行的商业活动。
- 管理员仍可查看数据和定位问题。

### 11.3 回滚触发条件

出现以下任一情况，应立即关闭广告展示或暂停活动：

- 查询成功率下降。
- Telegram 429 明显增加。
- 广告链接被举报或出现跳转异常。
- 退订率异常升高。
- 出现重复大规模发送。
- 广告内容与审核版本不一致。
- 发现用户链上数据被用于定向。

## 十二、验收标准

### 阶段 A 验收

- 广告默认关闭，开启后才展示。
- 每次查询最多展示一条广告。
- 查询结果、汇率和笔数不受广告逻辑影响。
- 广告明确显示“广告”或“赞助内容”。
- 广告链接必须为 HTTPS 且通过域名白名单。
- 广告服务异常时，查询结果仍正常返回。
- 可以统计展示次数和点击次数。

### 阶段 B 验收

- 用户必须主动确认订阅。
- 默认不接收商业消息。
- 用户可从设置、命令和广告按钮退订。
- 退订后不再发送新的商业广告。
- 频率限制跨重启仍然有效。
- 发送失败、429 和用户拉黑均有明确状态。

### 阶段 C 验收

- 非管理员无法创建、审核、启动或暂停广告活动。
- 批量发送前必须预览并二次确认。
- 活动可暂停、恢复和回滚。
- 每个用户的发送结果可追踪。
- 发送任务可在容器重启后恢复。
- 所有管理员操作都有审计记录。

## 十三、建议的开发顺序

1. 增加 `ADS_ENABLED` 开关和统一广告消息渲染器。
2. 实现查询结果赞助位，不做主动推送。
3. 增加 HTTPS、域名白名单、内容长度和状态校验。
4. 为广告展示和点击增加事件记录。
5. 将用户数据迁移到 SQLite，并增加订阅状态。
6. 实现设置页订阅/退订流程。
7. 实现持久化投递队列和限流 Worker。
8. 实现管理员广告活动命令和二次确认。
9. 加入 CI 测试、限流测试、镜像扫描和灰度发布。
10. 完成备份恢复和广告系统回滚演练。

## 十四、结论

广告功能可以集成到当前项目，但建议采用“先结果页赞助位，后自愿订阅推送，最后管理员活动平台”的路线。

第一期的核心原则是：广告逻辑不能影响收入统计；商业消息不能默认发送；用户链上数据不能用于广告定向；任何批量发送都必须有权限、限流、退订、审计和回滚能力。

只要按照上述阶段推进，项目可以在保持当前 MVP 简洁性的同时，为后续商业化预留清晰、可控的扩展空间。
